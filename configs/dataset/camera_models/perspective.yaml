# Perspective camera model parameters
model: perspective
params:
  fx: 1000.0
  fy: 1000.0
  cx: 500.0
  cy: 500.0
description: >
  Standard pinhole camera model used in Human3.6M.
  Projects 3D (x, y, z) to 2D (u, v) using:
    u = fx * (x / z) + cx
    v = fy * (y / z) + cy
